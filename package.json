{
  "name": "redis-v-oracle-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Side-by-side counterparty search: Redis Query Engine vs Oracle Database",
  "main": "src/server.js",
  "scripts": {
    "generate": "node src/generate.js",
    "seed:redis": "node src/seed-redis.js",
    "seed": "npm run generate && npm run seed:redis && npm run seed:oracle",
    "seed:1m": "COUNT=1000000 npm run seed",
    "start": "node src/server.js",
    "bench": "node src/bench.js",
    "demo": "docker compose up -d && npm run seed && npm start",
    "embed": "node src/embed.js",
    "seed:vectors": "npm run generate && npm run embed && npm run seed:redis && npm run seed:oracle",
    "validate": "node src/validate.js",
    "seed:oracle": "node src/seed-oracle.js"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "express": "^4.21.2",
    "oracledb": "^7.0.1",
    "redis": "^4.7.1"
  }
}
